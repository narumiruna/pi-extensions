---
"@narumitw/pi-sync": minor
---

Bundle a safety-first skill that guides installation, backend setup, validation, and the first reviewed sync. Support masked session-token entry for temporary S3 and R2 credentials in setup and storage-connection management, rejecting whitespace-only tokens before saving. Keep newly added S3 and WebDAV setups manual until automatic sync is explicitly enabled, and guide exact destination review and safe current-setup selection.
