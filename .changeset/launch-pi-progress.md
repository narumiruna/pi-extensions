---
"@narumitw/pi-progress": minor
---

Replace the Todo package and tool with `@narumitw/pi-progress` and the canonical `update_progress` `{ steps: [{ text, status, reason? }] }` contract while preserving valid historical session state, compaction boundaries, and read-only legacy settings fallback.
