---
"@narumitw/pi-starship": patch
---

Avoid periodic TUI redraw requests when cached footer snapshots are unchanged, while preserving clock updates without rescanning session history on idle ticks.
